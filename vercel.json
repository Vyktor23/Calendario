{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "npm install --prefix chronos",
  "buildCommand": "npm run build --prefix chronos",
  "outputDirectory": "chronos/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

{
  "name": "calendario",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm run build --prefix chronos",
    "vercel-build": "npm install --prefix chronos && npm run build --prefix chronos"
  }
}
